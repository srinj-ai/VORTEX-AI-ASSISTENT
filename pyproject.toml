[project]
name = "vortex-ai"
version = "0.1.0"
description = "VORTEX AI FastAPI app"
requires-python = ">=3.10"
dependencies = [
  "openai>=1.0.0",
  "fastapi",
  "uvicorn",
]

[tool.vercel]
entrypoint = "api:app"
